class Fibonacci {
	public:
	/**
		Returns the nth-fibonacci number. 
		e.g. get(1) = 1
				 get(3) = 2
				 get(6) = 8

		This really should be a static function,
		but we'll let it be for now :)
	*/
	int get(int n);	
};
